networks:
  nas_core_net:
    external: true

services:
  # 로컬 AI 구동 엔진 (Ollama)
  ai-ollama:
    image: ollama/ollama:latest
    container_name: ai_ollama
    restart: always
    networks:
      - nas_core_net
    ports:
      - "11434:11434"
    volumes:
      - ./ollama_storage:/root/.ollama
    environment:
      - TZ=Asia/Seoul
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_NOPRUNE=1

  # ChatGPT 스타일 웹 인터페이스 (Open WebUI)
  ai-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: ai_webui
    restart: always
    networks:
      - nas_core_net
    ports:
      - "3000:8080"
    volumes:
      - ./webui_storage:/app/backend/data
    environment:
      - TZ=Asia/Seoul
      - OLLAMA_BASE_URL=http://ai-ollama:11434
    depends_on:
      - ai-ollama
